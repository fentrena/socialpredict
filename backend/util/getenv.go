package util

// GetEnv is kept as a placeholder for future env validation logic.
// In production, Fly.io secrets are injected automatically.
// No need to load a .env file manually.

func GetEnv() error {
    return nil
}
